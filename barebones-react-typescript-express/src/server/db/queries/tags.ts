/* import { Query } from "../";

const all = () =>
  Query(`
    SELECT
        blogs.title,
        blogs.content,
        authors.name
    FROM blogs
    JOIN authors ON authors.id = blogs.id
`);

const one = () =>
  Query(`
    SELECT
        blogs.title,
        blogs.content,
        authors.name
    FROM blogs
    JOIN authors ON authors.id = blogs.id
`);


export default {
  all,
  one
}; */
